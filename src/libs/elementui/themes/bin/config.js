const themePath = 'src/libs/elementui/themes';

const conf = {
  varPath: `${themePath}/elementVariables.scss`,
  themeName: 'default',
  themePath: `${themePath}`,
  scope: false,
  minimize: true,
};

module.exports = conf;
